0.we use sestatus to check level of enforcement
1.we use aa-status to check AppArmor status
2.we use semanage command with -l (list) prefix to list out all ports managed by SELINUX and also grep and list only http related services
